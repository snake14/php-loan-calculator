{
    "name": "snake14/php-loan-calculator",
    "description": "This provides REST endpoints for performing some loan calculations and a UI.",
    "require": {
        "bcosca/fatfree-core": "^3.7"
    },
	"autoload": {
        "classmap": [
            "src/"
        ]
    },
    "scripts": {
        "start": "php -S localhost:7009 -t public",
		"test": "./vendor/bin/phpunit tests"
    },
	"config": {
		"process-timeout": 36000
	},
    "require-dev": {
        "phpunit/phpunit": "^9"
    }
}
